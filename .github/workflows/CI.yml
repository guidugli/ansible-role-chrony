name: CI

on:
  push:
  pull_request:

jobs:
  molecule:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        scenario: [default]

    steps:
      - uses: actions/checkout@v4

      - name: Install system deps (podman + python)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y podman python3 python3-venv

      - name: Setup venv
        run: |
          python3 -m venv .venv
          . .venv/bin/activate
          pip install -r requirements-dev.txt
          ansible-galaxy collection install -r requirements.yml

      - name: Update matrix + inventories
        run: |
          . .venv/bin/activate
          python3 scripts/update_matrix.py

      - name: Run Molecule
        run: |
          . .venv/bin/activate
          molecule test -s ${{ matrix.scenario }}
