---
# Generated file. Do not edit directly.
# Source template: {{ template_name }}

dependencies: []

galaxy_info:
  role_name: auto_update
  namespace: guidugli
  author: guidugli
  description: >-
    Install and configure automatic package update tooling on Debian, Ubuntu,
    and Fedora systems.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
{% for platform in platforms %}
    - name: {{ platform.name }}
      versions:
{% for version in platform.versions %}
        - {{ version | tojson }}
{% endfor %}
{% endfor %}
  galaxy_tags:
    - updates
    - automatic
    - patching
    - security
    - unattended
    - apt
    - dnf
    - linux
    - maintenance
